import styled from 'styled-components';

export const Copyright = styled.div`
	position: absolute;
	left: 0;
	width: 100vw;
    height: 8rem;
	display: grid;
	background-color: var(--clr-gold);
	margin-top: 4rem;
	padding: 8rem auto;
	place-items: center;
`;
